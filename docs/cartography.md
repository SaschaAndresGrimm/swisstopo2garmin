# Cartography — swissTLM3D to Garmin mapping

Authoritative mapping table (SPEC.md §8.2). Source values come from
[tlm3d-schema.md](tlm3d-schema.md); nothing here is invented.

## The rule that bites

> mkgmap `doc/typ-compiler.txt`: *"If a polygon type is not listed in
> [_drawOrder], then it will not be displayed at all."*

Attaching a TYP without a `[_drawOrder]` section hides **every polygon** — forest,
rock, glacier, water, buildings. The map still lists on the device and lines still
draw, so it looks like a rendering failure rather than a styling mistake. The IMG
content is unaffected: the polygons are present in the RGN either way, which is why
container inspection cannot detect this. `spikes/s0/checkstyle.py` guards it.

Extended type ranges used here: **lines `0x101xx`, polygons `0x102xx`.** Garmin keeps
separate namespaces for lines and polygons, but splitting the ranges avoids confusion.

## Draw order

Painted back to front; a higher number is drawn on top.

| Order | Content |
|---:|---|
| 1 | map background sheet (`0x4a`, `0x4b`) — generated by mkgmap, not the style |
| 2 | bare ground: rock, scree |
| 3 | ice and permanent snow |
| 4 | woodland |
| 5 | wetland, vineyards, orchards |
| 6 | built-up land use |
| 7 | water — above all land cover |
| 8 | buildings |

## Polygons

| TLM source | `objektart` | Type | Colour | Landeskarte intent |
|---|---|---|---|---|
| `tlm_bb_bodenbedeckung` | `Fels`, `Fels locker`, `Felsbloecke`, `Felsbloecke locker` | `0x10201` | `#E0DCD3` | rock grey |
| | `Lockergestein`, `Lockergestein locker` | `0x10202` | `#EDE9E0` | scree, lighter |
| | `Gletscher` | `0x10203` | `#DAEEF7` | glacier blue-white |
| | `Schneefeld Toteis` | `0x10204` | `#EAF6FB` | firn, paler |
| | `Wald` | `0x10205` | `#BCDFA6` | closed forest green |
| | `Gehoelzflaeche` | `0x10206` | `#CDE7B8` | copse |
| | `Gebueschwald` | `0x10207` | `#D8EDC6` | scrub |
| | `Wald offen` | `0x10208` | `#D2E9BD` | open forest |
| | `Feuchtgebiet` | `0x10209` | `#C4E2DA` | wetland |
| | `Stehende Gewaesser` | `0x1020f` | `#AAD6EF` | lake blue |
| | `Fliessgewaesser` | `0x10210` | `#AAD6EF` | wide river |
| `tlm_areale_nutzungsareal` | `Reben` | `0x1020a` | `#E3EABB` | vineyard |
| | `Obstanlage`, `Baumschule` | `0x1020b` | `#DAEBBD` | orchard, nursery |
| | *(all other)* | `0x1020c` | `#EEEAE3` | institutional grounds |
| `tlm_areale_verkehrsareal` | *(all)* | `0x1020d` | `#E7E3DC` | parking, transport |
| `tlm_areale_freizeitareal` | *(all)* | `0x1020e` | `#DCEAD1` | sports, leisure |
| `tlm_bauten_gebaeude_footprint` | — | `0x10211` | `#8C837A` | buildings |

**Note the coverage model.** swissTLM3D land cover carries only *natural* classes.
There is no polygon for meadow, farmland or ordinary settlement — those are the white
background. This is exactly how the Landeskarte works, so the background sheet colour
(`#FFFFFF`) is doing real cartographic work, not just filling a void.

## Lines

| TLM source | Value | Type | Colour / casing |
|---|---|---|---|
| `tlm_strassen_strasse.wanderwege` | `Wanderweg` | `0x10101` | `#FFD100` / `#7A6500` — yellow |
| | `Bergwanderweg` | `0x10102` | `#E2001A` / `#FFFFFF` — red-white |
| | `Alpinwanderweg` | `0x10103` | `#0067B2` / `#FFFFFF` — blue-white |
| `…objektart` | `Klettersteig` | `0x10104` | black / red — via ferrata |
| | `Autobahn` | `0x01` | `#E8823C` |
| | `Autostrasse` | `0x02` | `#EE9A55` |
| | `10m Strasse` | `0x03` | `#F5B461` |
| | `8m Strasse` | `0x04` | `#FBD98B` |
| | `6m Strasse` | `0x05` | `#FDECBE` |
| | `4m / 3m Strasse` | `0x06` | white, grey casing |
| | `Verbindung`, `Zufahrt`, `Dienstzufahrt`, `Platz` | `0x07` | white, light casing |
| | `2m Weg` | `0x0a` | white, brown casing |
| | `1m Weg`, `Wegfragment`, `Markierte Spur` | `0x16` | `#6E6257` thin |
| | `Einfahrt`, `Ausfahrt` | `0x08` | ramp orange |
| | `Faehre` | `0x1a` | blue |
| `tlm_oev_eisenbahn` | — | `0x14` | black / white |
| `tlm_gewaesser_fliessgewaesser` | `Fluss` | `0x1f` | `#5FAEDC` |
| | *(other)* | `0x18` | `#74BCE4` |
| contours (swissALTI3D) | minor / medium / major | `0x20` / `0x21` / `0x22` | bistre `#C9A277` → `#A87642` |

Hiking rules are matched **before** the road rules and use `continue with_actions`, so a
trail keeps its class styling while still contributing its underlying road geometry.

## Known gaps

- **No night palette.** Polygons use a single colour (same day and night). FR-CART2 is
  unmet. Adding it needs the 2-colour polygon form verified on hardware first.
- **No patterns.** Landeskarte rock hachures, wetland ticks and vineyard rows are flat
  fills here. TYP supports XPM bitmaps for these.
- **No separate wrist variant.** FR-CART6 unmet — line weights are tuned for an Edge.
- **Contours are one colour** everywhere; the Landeskarte varies them over rock and ice.
- **Partly verified.** The summer palette renders correctly on an Edge 840 (firmware
  3133) and a fēnix 5 Plus (firmware 1930); see the log in
  [device-verification.md](device-verification.md). The **winter palette, slope hatches
  and night palette have never been on a device.** The night palette is the one to
  distrust: it is the only palette here derived — by inverting the day colours under
  contrast rules — rather than measured from a swisstopo sheet.

---

## Generation

`typ/swisstopo.txt` is **generated** — do not edit it. Edit
[`cartography/palette.json`](../cartography/palette.json) (colours) or
[`tools/make_typ.py`](../tools/make_typ.py) (patterns, type tables), then run:

```
python3 tools/make_typ.py && python3 spikes/s0/checkstyle.py
```

`build.sh` does both and fails the build on a style/TYP mismatch.

## Relief shading (FR-CART8)

DEM data is embedded in the map so the device renders shaded relief natively — the
largest single step toward the raster Landeskarte look.

- Source: **`ch.swisstopo.swissaltiregio`** — one national 10 m COG, 55000×43000 px,
  EPSG:2056, 5 overview levels, 10.11 GB but read by range request.
  swissALTI3D (2 m) is *not* usable here: one 1°×1° `.hgt` cell would mean reading
  ~10 GB of 2 m tiles, versus a few MB from swissALTIRegio's overviews.
- `spikes/s0/make_dem.py` warps it into SRTM `.hgt` cells (1 arc-second → 3601×3601,
  25,934,402 B each) via GDAL's `SRTMHGT` driver.
- mkgmap: `--dem=<dir> --dem-dists=3314,6628,13256,26512,53024` — one distance per
  entry in `--levels`. Adds ~230 KB per map tile and is purely additive: LBL, RGN and
  TRE are byte-identical with and without it.

## Pattern fills (FR-CART10)

Rock is not a grey area in the Landeskarte — it is dark hachures on white paper. Four
32×32 XPM patterns are generated by `tools/make_typ.py`: rock hachure, scree stipple,
wetland ticks, vineyard rows. Each adds 128 bytes to the TYP.

> **Preview caveat.** `tools/render.py` cannot draw TYP bitmap patterns. It approximates
> a pattern by blending background and ink at the pattern's coverage ratio, so previews
> show a flat tint where the device shows texture. Judge patterns on hardware.

## Hydrography exclusions

swissTLM3D's watercourse layer carries constructs the Landeskarte does not draw. Leaving
them in produced long straight blue lines cutting across lakes and glaciers:

| Excluded | Count (national) | Why |
|---|---:|---|
| `objektart=Seeachse` | 11,239 | lake axis — a connectivity line through a water body |
| `verlauf=Unterirdisch*` | 137,832 | piped or subterranean |
| `Druckstollen`, `Druckleitung*` | 1,350 | pressure tunnels and penstocks |

Dropped with `{ deletealltags }`, which is mkgmap's idiom for discarding an element —
there is no "type 0x00 means discard".

---

## Contributing a cartography change

Cartography is the one part of this project where the tests cannot tell you whether the
answer is right. They can only tell you it is *consistent*. So the workflow is built
around making a change reviewable rather than around proving it correct.

### The two-file rule

`typ/swisstopo.txt` is generated. Every colour change is an edit to
[`cartography/palette.json`](../cartography/palette.json); every pattern, icon or type
table change is an edit to [`tools/make_typ.py`](../tools/make_typ.py). Then:

```sh
python3 tools/make_typ.py          # regenerate the TYP
python3 spikes/s0/checkstyle.py    # every emitted type is drawn, and in a draw order
python3 tools/check_i18n.py        # if you added a UI string for it
```

CI regenerates the TYP and fails if the committed one differs, so a hand edit to
`typ/swisstopo.txt` is caught rather than silently overwritten later.

`checkstyle.py` catches the failure mode that costs the most time: a type the style
emits that the TYP's `[_drawOrder]` does not mention is **invisible on the device and
invisible in the `.img`**. Nothing else finds it — see m0-findings §4.8.

### Where a colour is allowed to come from

Measured from a swisstopo product, or derived from something measured, and the source
recorded. `tools/sample_palette.py` and `tools/winter_palette.py` sample the national map
raster; `tools/slope_palette.py` and `tools/night_palette.py` derive from what those
produced, under stated rules.

An invented colour that looks right on a monitor is the thing this discipline exists to
prevent, because the device's screen is not the monitor and the reviewer's judgement of a
hex value in a diff is worth very little.

### Showing your work

`tools/visual_regression.py` renders fixed extents through the offline renderer and diffs
against committed PNGs. Include the before and after images in the pull request — this is
what makes a cartography change reviewable by somebody who is not looking at your screen.

Two caveats the renderer cannot get past, both of which mean **judge on hardware**:

* It cannot draw TYP bitmap patterns. It approximates one by blending background and ink
  at the pattern's coverage ratio, so rock shows as a flat tint where the device shows
  hachures.
* It is not a Garmin. Line weights, label placement and the device's own relief shading
  are all its own.

### Before it ships

Add the change to section B of [device-verification.md](device-verification.md) with a
sentence saying what specifically needs somebody's eyes and why a renderer cannot answer
it. A cartography change with no hardware check queued behind it is a change nobody knows
the effect of.
